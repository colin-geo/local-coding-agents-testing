# Model files — too large for git
models/

# Default agent workspace — contents are user projects, not part of this repo
workspace/*
!workspace/.gitkeep

# LiteLLM runtime state
*.db
litellm_proxy_config*.db

# Environment overrides
.env

# Docker compose local overrides
docker-compose.override.yml

# Logs
*.log

# Python cache
__pycache__/
*.pyc
.aider*
